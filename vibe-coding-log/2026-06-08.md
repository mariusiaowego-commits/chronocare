# Vibe Coding Log — ChronoCare

## 2026-06-08

**Session focus:** tjh 就诊记录批量导入 (49 PDFs → 49 MedicalRecord + 41 Visit)

**Done:**
- 49 个 PDF 全部按就诊日期下载并重命名 `tjh病例-老年医院-yymmdd.pdf` (8 组同日期加 `-1`/`-2` 后缀)
- 解析策略: `pdftotext` → 正则提取 `就诊时间`/`科室`/`诊 断`/`处理`/etc
- dry-run 通过 49/49 (0 警告)
- 修复 `诊 断` 正则 bug：原 `_DIAG_RE` 会误抓「现病史」段里的「，诊断：1.xxxx」(旧检查报告引用)，改为 `^或换行 + 诊 断：` 才匹配
- 真实导入: 49 MedicalRecord + 41 Visit (按用户选「全保留」方案)
- 修复 `VisitRead.attachments: dict → list[str]` (schema bug, 之前 attachments=None 没触发，我们导入 list 后 500)
- 端到端验证: `/api/visits?person_id=2` 返回 41 条、`/api/medical-records/18` 返回完整 JSON

**Git:**
- 分支: `feature/tjh-import-49-pdfs` (从 main `5a93976` 开)
- tag: `pre-tjh-import` (DB state 文档标记)
- DB 备份: `data/backups/chronocare-pre-tjh-import-20260608-171018.db` + `/tmp/chronocare-pre-tjh-import.db`

**回退路径:**
- 代码回退: `git checkout main && git branch -D feature/tjh-import-49-pdfs`
- DB 回退: `uv run python scripts/import_tjh_pdfs.py --rollback`
- import 脚本幂等: 同 image_path 不重复写

**Discovered (out of scope, 不在这次 PR):**
- `data/tjh-pdfs/` 没被 mount 为静态目录，PDF 不能直接通过 URL 访问 (页面要展示 PDF 链接需在 main.py 加 mount)
- `visit_type` 全部写 `followup` (推断值)，少数 visit 可能是 `checkup` 或 `initial`，需业务侧人工复核
- `chief_complaint` 有 1 条是 `{emr_reference:诊断.诊断}随访` (HIS 模板变量未替换, 真实源数据问题)

**Next:**
- main.py mount `data/` (PDF 文件可访问)
- 业务复核 visit_type
- 后续 5 个未跟踪的 `scripts/download_*.py` 需要 scout 决定保留/丢弃
